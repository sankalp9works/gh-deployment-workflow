# gh-deployment-workflow

This project demonstrates a simple CI/CD pipeline using GitHub Actions.

Whenever `index.html` is updated and pushed to the `main` branch,
the `deploy.yml` workflow automatically publishes the website to GitHub Pages.


URL: https://sankalp9works.github.io/gh-deployment-workflow/
